@page "/order/payment-success"
@layout MainLayout

<section>
    <div class="w-full bg-[#9797971c] py-6">
        <div class="mx-auto w-full max-w-[1520px] px-6">
            <h1 class="mb-2.5 font-medium leading-tight text-[44px] text-white">Thanh Toán</h1>
            <MudBreadcrumbs Class="custom-breadcrumb !p-0 !text-xs" Items="_itemBreadcrumbs" Separator="/" />
        </div>
    </div>
    <div class="flex w-full flex-col items-center justify-center gap-4 rounded-lg border border-solid border-slate-200 p-6 text-center shadow">
        <MudIcon Icon="@Icons.Material.Filled.CheckCircle" Color="Color.Primary" Class="!text-4xl" />
        <h2 class="text-2xl font-bold text-white">Đặt hàng thành công!</h2>
        <p class="text-base"><span class="font-bold">Mã hóa đơn:</span> @_orderCodeSuccess</p>
        <div class="flex justify-center gap-3">
            <MudButton Variant="Variant.Filled" Color="Color.Primary" OnClick="@(() => navigation.NavigateTo(RouterConfig.Customer.Home))">Tiếp tục mua sắm</MudButton>
            <MudButton Variant="Variant.Outlined" OnClick="@(() => navigation.NavigateTo(RouterConfig.Customer.OrderDetail.Replace(":id", _orderIdSuccess.ToString())))">Xem hóa đơn</MudButton>
        </div>
    </div>
</section>

@code {
    [SupplyParameterFromQuery(Name = "order-id")]
    public int _orderId { get; set; } = 0;

    private int _orderIdSuccess = -1;
    private string _orderCodeSuccess = string.Empty;

    private List<BreadcrumbItem> _itemBreadcrumbs = [
        new("TRANG CHỦ", href: RouterConfig.Customer.Home),
        new("THANH TOÁN", href: null, disabled: true),
    ];

    protected override async Task OnInitializedAsync()
    {
        await LoadData();
        StateHasChanged();
    }

    async Task LoadData()
    {
        if (_orderId > 0)
        {
            _orderIdSuccess = _orderId;
            var result = await _orderService.PaymentSuccess(_orderId);
            if (result.IsSuccess)
            {
                _orderCodeSuccess = result.Data.OrderCode;
            }
        }
    }
}
